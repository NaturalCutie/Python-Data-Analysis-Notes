"""
条件语句结构:
if [条件]:
    [执行语句]  # 执行语句前要有缩进,建议缩进为4个空格。有缩进的内容会判断为条件为真时要执行的内容，无缩进会认为if语句结束。
    [执行语句]  # 执行语句可以为多个  # 假如条件为假时什么都不做，可以省略else及后面的内容
else:
    [执行语句]
    [执行语句]
"""

# "条件"需求值为布尔值，包括：
# 1.类型是布尔值的变量。
# 2.结合比较运算符的算式，包括：
# 1) == 2) != 3) > 4) < 5) >= 6) <=

mood_index = int(input("宝宝今天的心情指数："))
if mood_index >= 60:
    print("可以睡个安稳觉了")
    print("(*^▽^*)")
else:
    print("准备好战斗吧!")
